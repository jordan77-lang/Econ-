# ECN212 Mission Memo and Canvas Corrections

Use the language below to resolve the two content inconsistencies found during final review.

## 1) Question 11 Interval Mismatch (Critical)

Issue: The prompt references the 960 to 1,120 interval, while the worked feedback and site logic use a different interval.

Recommended final wording (Q11 prompt):

- Question text:
  - What is the marginal cost per bottle across the interval from 960 to 1,200 bottles?
- Response type:
  - Numerical Answer (+/- 0.005)

Recommended feedback text (Q11):

- Answer: $0.219
- To calculate marginal cost, divide the change in total cost by the change in output.
- MC = Delta TC / Delta Q
- For the interval from 960 to 1,200 bottles:
  - MC = ($285.50 - $233.00) / (1,200 - 960)
  - MC = $52.50 / 240
  - MC = $0.219

Note: This aligns with the terminal table outputs used throughout the activity.

## 2) Question 33 Key Logic (Consistency)

Issue: Option e is economically true at Q=1,200 (price > ATC), but is excluded from the listed correct set.

Recommended final wording (Q33 options and key):

- Keep options a through e as written.
- Mark correct options as:
  - a, c, d, e

Recommended feedback text (Q33):

- Answer: a, c, d, and e.
- These statements jointly support 1,200 as the best strategy:
  - ATC is at (or in) its minimum range near this output.
  - MC is closest to MR without exceeding it.
  - Profit is highest at this output.
  - Price exceeds ATC at this output, so total costs are covered in the long run.
- Option b is not a valid justification because maximum total revenue alone does not identify the best strategy when costs are changing with output.

## Optional Instructor Note (if you want to avoid over-determination)

If you prefer exactly three correct statements for Q33, revise option e wording to make it non-overlapping with a, c, and d. Example revision:

- e. At that output, price exceeds ATC by the largest margin among listed outputs.

Then keep a, c, d as the correct set.
